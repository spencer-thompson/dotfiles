interface:
  display_name: "Set Up Evals"
  short_description: "Install a project-owned evidence loop"
  default_prompt: "Use $setup-evals to install a development-only evaluation system and propose project agent policies."

policy:
  allow_implicit_invocation: false
