interface:
  display_name: "Session Recall"
  short_description: "Find past Codex chats, decisions, and work"
  default_prompt: "Use $session-recall to find relevant past chats and summarize the evidence, decisions, and open work."

policy:
  allow_implicit_invocation: false
